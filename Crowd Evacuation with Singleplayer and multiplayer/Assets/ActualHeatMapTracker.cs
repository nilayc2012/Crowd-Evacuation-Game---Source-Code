using UnityEngine;
using System;
using System.Collections;
using System.Collections.Generic;



public class ActualHeatMapTracker : MonoBehaviour
{
    //List<PosTime> positions;
   // int timer;
    //float timeIncrement = 0;
    // Use this for initialization
    void Start()
    {
       /* positions = new List<PosTime>();
        GameController.agentPositionMap[this.gameObject.name] = positions;
        timer = 0;
        timeIncrement = 0;*/
    }


    // Update is called once per frame
    void LateUpdate()
    {/*
        timeIncrement = timeIncrement + Time.fixedDeltaTime;
        GameController.agentPositionMap[this.gameObject.name].Add(new PosTime (timeIncrement, this.transform.position));

        */
    }

}






